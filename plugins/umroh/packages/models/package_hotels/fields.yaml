# ===================================
#  Form Field Definitions
# ===================================

fields:
    hotel:
        label: Hotel
        type: relation
        nameFrom: name
        emptyOption: "-- Silahkan Pilih Hotel --"
    check_in_date:
        label: Tanggal Check In
        type: datepicker
        mode: date
    check_out_date:
        label: Tanggal Check Out
        type: datepicker
        mode: date
